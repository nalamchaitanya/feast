function keysch = keyExp(key,rnds)
    // key is 64 bits so remove parity bits.
    // then generate 'rnds' no of keys in order
    // and store in a matrix 'keysch' which is of
    // dimensions [rnds X 24] as 24 is our key size
    // for each round.
end
